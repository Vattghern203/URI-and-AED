#include <bits/stdc++.h>

int main(){
	char op;
	double matriz[12][12];
	int c,l,soma,media;
	
	scanf("%c",&op);
	
	scanf("%lf",&matriz[l][c]);
	
	if(op == 'S'){
		for(l = 0;l < 12;l++){
			for(c = 0;c < 12;c++){
				soma = soma + matriz[]
			}
		}
	}
}
